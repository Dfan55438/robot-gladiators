// this creates a function named "fight"

var playerName = window.prompt("What is your robot's name?");
console.log("Player " + playerName + " has entered the fight!");
//console.log used during testing so that window.alert doesnt affect end users
console.log

function fight()
    {
        window.alert("The fight has begun!");
    }

    
    //fight();
